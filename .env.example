# Address of the backend API (no trailing slash).
# Local development: the backend started with `npm run dev` in the backend folder.
# Production: https://api.your-domain.com — the value is built into `npm run build`.
VITE_API_BASE_URL=http://localhost:4000

# This dashboard runs on http://localhost:5174 (the backend's CORS_ORIGINS must list it).
